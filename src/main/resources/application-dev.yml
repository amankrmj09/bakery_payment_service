spring:
  datasource:
    url: jdbc:postgresql://blubug.me:8004/bakery_payments
    username: payment_user
    password: payment_password
  kafka:
spring:
  datasource:
    url: jdbc:postgresql://blubug.me:8004/bakery_payments
    username: payment_user
    password: payment_password
  kafka:
    bootstrap-servers: localhost:9092
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
payment:
  gateways: {}
  limits:
    min-amount: 50.00
    max-amount: 500000.00
    daily-limit: 1000000.00
